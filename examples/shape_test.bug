num main() {
    num cx;
    num cy;

    cx = 0;
    cy = 0;
    num w;
    w = 1500;

    num h;
    num x;
    num y;
    h = 1000;
    x = w / 2;
    y = h / 2;
    num g;
    g = 100;
    string stroke;
    stroke = "0.0 0.3 0.5";
    string fill;
    fill = "0.2 0.2 0.2";
    string id;
    id = "";

    num xt;
    xt = 800;
    num yt;
    yt = 800;

    num thickness;
    thickness = 5;

    init_canvas();
    // add_ccanvas(c, d, a, b);
    id = add_circle(x, y, g, stroke, thickness, fill, id);
    // add_point_xy(c, d, id);
    moveById(id, xt, yt, thickness);
    add_canvas(w, h, cx, cy);


    return 0;
}
